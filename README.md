# پلاگین آزمایشگاه پوش

پلاگین کامل برای سیستم لاگین و ثبت‌نام با استفاده از پیامک و وردپرس.

## ویژگی‌ها

### 🔐 سیستم احراز هویت
- لاگین و ثبت‌نام با شماره موبایل
- تایید هویت با کد پیامک
- پشتیبانی از کد تست "939393"
- مدیریت session با وردپرس

### 📱 اتصال به ملی پیامک
- ارسال کد تایید از طریق API ملی پیامک
- ذخیره کدهای OTP در wp_options
- انقضای خودکار کدها (2 دقیقه)
- محدودیت تعداد تلاش (3 بار)

### 📊 سیستم لاگ
- ثبت 50 لاگ آخر در wp_options
- لاگ‌های مختلف: ارسال پیامک، لاگین، ثبت‌نام، تایید کد
- نمایش آمار در پنل ادمین
- امکان پاک کردن لاگ‌ها

### ⚙️ پنل تنظیمات ادمین
- تنظیمات ملی پیامک (نام کاربری، رمز عبور، شماره ارسال‌کننده)
- تنظیمات صفحه لاگین (لوگو، متن‌ها)
- نمایش و مدیریت لاگ‌ها
- تست اتصال پیامک

### 🎨 رابط کاربری
- طراحی ریسپانسیو با Tailwind CSS
- پشتیبانی از فارسی و راست به چپ
- انیمیشن‌ها و تعاملات نرم
- سازگار با موبایل

## نصب و راه‌اندازی

### 1. نصب پلاگین
```bash
# کپی کردن فایل‌ها در پوشه پلاگین‌های وردپرس
wp-content/plugins/arta-poyeshLab/
```

### 2. فعال‌سازی پلاگین
- وارد پنل ادمین وردپرس شوید
- به بخش "پلاگین‌ها" بروید
- پلاگین "Arta PoyeshLab" را فعال کنید

### 3. تنظیمات اولیه
- به منوی "آزمایشگاه پوش" در پنل ادمین بروید
- تنظیمات ملی پیامک را تکمیل کنید:
  - نام کاربری پنل ملی پیامک
  - رمز عبور پنل ملی پیامک
  - شماره ارسال‌کننده
- تنظیمات صفحه لاگین را سفارشی کنید:
  - آپلود لوگو
  - تغییر متن‌ها

### 4. تست سیستم
- به آدرس `/lab-portal` بروید
- فرآیند ثبت‌نام یا لاگین را تست کنید
- از کد "939393" برای تست استفاده کنید

## استفاده

### برای کاربران
1. به آدرس `/lab-portal` بروید
2. برای ثبت‌نام: اطلاعات خود را وارد کنید و کد تایید را دریافت کنید
3. برای لاگین: شماره موبایل خود را وارد کنید و کد تایید را دریافت کنید
4. کد 6 رقمی را وارد کنید تا وارد سیستم شوید

### برای ادمین‌ها
1. در پنل ادمین، منوی "آزمایشگاه پوش" را باز کنید
2. تنظیمات پیامک را تکمیل کنید
3. لوگو و متن‌های صفحه لاگین را سفارشی کنید
4. لاگ‌های سیستم را در بخش "لاگ سیستم" مشاهده کنید

## ساختار فایل‌ها

```
arta-poyeshLab/
├── arta-poyeshLab.php          # فایل اصلی پلاگین
├── include/
│   ├── apl-main.php            # کلاس اصلی
│   ├── function.php            # توابع کمکی
│   ├── classes/
│   │   ├── apl-sms-handler.php # مدیریت پیامک
│   │   ├── apl-logger.php      # سیستم لاگ
│   │   ├── apl-admin-settings.php # تنظیمات ادمین
│   │   ├── apl-ajax-handlers.php  # AJAX handlers
│   │   ├── apl-auth.php        # احراز هویت
│   │   ├── apl-cron.php        # کارهای زمان‌بندی شده
│   │   └── apl-my-account.php  # مدیریت حساب کاربری
│   └── template/
│       ├── layout.php          # قالب اصلی
│       ├── auth.php            # صفحه لاگین/ثبت‌نام
│       └── dashbord.php        # داشبورد کاربر
└── assets/
    ├── css/
    │   └── style.css           # استایل‌های سفارشی
    └── js/
        └── script.js           # اسکریپت‌های JavaScript
```

## تنظیمات پیشرفته

### تغییر زمان انقضای کد OTP
در فایل `include/classes/apl-sms-handler.php`:
```php
'expires' => time() + 120, // 2 دقیقه (120 ثانیه)
```

### تغییر تعداد لاگ‌های ذخیره شده
در فایل `include/classes/apl-logger.php`:
```php
private $max_logs = 50; // تعداد لاگ‌های ذخیره شده
```

### تغییر محدودیت تلاش برای کد OTP
در فایل `include/classes/apl-sms-handler.php`:
```php
if ($otp_data['attempts'] >= 3) { // 3 تلاش
```

## عیب‌یابی

### مشکل در ارسال پیامک
1. تنظیمات ملی پیامک را بررسی کنید
2. از بخش "تست اتصال" در پنل ادمین استفاده کنید
3. لاگ‌های سیستم را بررسی کنید

### مشکل در لاگین
1. مطمئن شوید کاربر در دیتابیس وجود دارد
2. کد OTP را بررسی کنید
3. از کد تست "939393" استفاده کنید

### مشکل در نمایش صفحه
1. مطمئن شوید پلاگین فعال است
2. فایل‌های CSS و JS بارگذاری شده‌اند
3. دسترسی‌های فایل‌ها را بررسی کنید

## پشتیبانی

برای پشتیبانی و گزارش باگ، با تیم توسعه تماس بگیرید.

## مجوز

این پلاگین تحت مجوز GPL v2 یا بالاتر منتشر شده است.
